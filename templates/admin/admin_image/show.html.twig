{% extends 'admin/base.html.twig' %}

{% block title %}Image{% endblock %}

{% block body %}
    <h1>Image</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ image.name }}</td>
            </tr>
            <tr>
                <th>IsMain</th>
                <td>{{ image.isMain ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_image_index') }}">back to list</a>

    <a href="{{ path('app_admin_image_edit', {'id': image.id}) }}">edit</a>

    {{ include('admin/admin_image/_delete_form.html.twig') }}
{% endblock %}
